//Promise<IBXSuccessResponse<T>>
export function fetchRestAPI<T>(method: string, filter: Object = {}){
    // return new Promise((resolve, reject) => {
    //     bitrix.callMethod(method, filter, (r) => {
    //         if('error' in r){
    //             reject(new Error(r.error))
    //         } else{
    //             resolve(r)
    //         }
    //     }, reject)
    // })
}